PyMuPDF>=1.26.0
pymupdf4llm>=0.0.20

requests>=2.32.0

faiss-cpu>=1.11.0

langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-text-splitters>=0.3.0

openai>=1.99.0

python-dotenv>=1.1.0

google-generativeai>=0.8.5

fastapi>=0.116.0
uvicorn[standard]>=0.35.0

pydantic>=2.11.0

python-docx>=1.2.0
openpyxl>=3.1.5
pandas>=2.3.0

pytesseract>=0.3.13
Pillow>=11.0.0

docx2txt>=0.9
xlrd>=2.0.1
python-pptx>=1.0.2

celery[redis]>=5.5.3
redis>=6.2.0
flower>=2.0.1

# --- Supabase Auth ---
PyJWT>=2.9.0          # JWT decode + HS256 verification (Supabase JWTs)
supabase>=2.9.0       # Python client — server-side only (user_profiles upsert)

# --- production hardening (no new packages needed) ---
# Rate limiting  → redis (already above) — sliding-window via INCR+EXPIRE
# Structured JSON logging → stdlib logging + json (no structlog needed)
# Circuit breaker → stdlib threading + concurrent.futures (no tenacity needed)
# Health check    → redis + celery[redis] (already above)